>>>>>> import argparse
>>>>>> import trace
>>>>>> from itertools import batched
       
       # Global counter for the number of states sifted through to remove duplicates
>>>>>> duplicatesSifted = 0
       
       # Global counter for the number of states created
>>>>>> statesCreated = 0
       
       # Global counter for the number of symbols processed
>>>>>> symbolsProcessed = 0
       
       
>>>>>> class State:
           """
           The State class contains the progress in matching the input signal to each
           of the reference signals
           """
       
>>>>>>     def __init__(
               self,
>>>>>>         i: int = 0,
>>>>>>         j: int = 0,
>>>>>>         labels: str = "",
>>>>>>         cx: int = 0,
>>>>>>         cy: int = 0,
>>>>>>         start: int = -1,
>>>>>>         end: int = -1,
>>>>>>         best: int = -1,
>>>>>>     ) -> None:
               """Initializes a new State"""
  307:         self.i = i
  307:         self.j = j
  307:         self.labels = labels
  307:         self.cx = cx
  307:         self.cy = cy
  307:         self.start = start
  307:         self.end = end
  307:         self.best = best
       
       
>>>>>> def removeDuplicates(states: list[State]) -> list[State]:
           """Removes duplicate States within a list of States"""
           global duplicatesSifted
       
   15:     seen = set()
   15:     results = []
       
           # Loop through all States
  322:     for state in states:
               # Create a tuple with all of the items that make a State unique to use
               # as a key
  307:         key = (state.i, state.j, state.cx, state.cy)
       
               # Mark the key as seen and append the State to the results list
  307:         if key not in seen:
  265:             seen.add(key)
  265:             results.append(state)
       
  307:         duplicatesSifted += 1
       
   15:     return results
       
       
>>>>>> def untangleSignals(
>>>>>>     s: str, x: str, y: str
>>>>>> ) -> tuple[list[list[int]], list[list[int]], list[int]]:
           """Performs the Untangle Signals algorithm"""
           global statesCreated
           global symbolsProcessed
       
           # List containing currently active States
    1:     active: list[State] = []
       
           # Loop through all symbols in the input signal
   16:     for position in range(len(s)):
   15:         symbol = s[position]
   15:         new: list[State] = []
       
               # New States
               # Beginning of X signal
   15:         if symbol == x[0]:
   16:             new.append(
   16:                 State(
    8:                     i=(1 % len(x)),
    8:                     j=(0),
    8:                     labels=("x"),
    8:                     cx=(1 // len(x)),
    8:                     cy=(0),
    8:                     start=(position),
    8:                     end=(-1),
    8:                     best=(-1),
                       )
                   )
    8:             statesCreated += 1
       
               # Beginning of Y signal
   15:         if symbol == y[0]:
   14:             new.append(
   14:                 State(
    7:                     i=(0),
    7:                     j=(1 % len(y)),
    7:                     labels=("y"),
    7:                     cx=(0),
    7:                     cy=(1 // len(y)),
    7:                     start=(position),
    7:                     end=(-1),
    7:                     best=(-1),
                       )
                   )
    7:             statesCreated += 1
       
               # Existing States
  245:         for state in active:
                   # End hasn't been found yet, keep evaluating against X and Y
  230:             if state.end == -1:
                       # Symbol is next for X
  222:                 if symbol == x[state.i]:
  135:                     newi = (state.i + 1) % len(x)
  135:                     newcx = state.cx + ((state.i + 1) // len(x))
  135:                     newbest = state.best
  135:                     if newi == 0 and state.j == 0 and newcx > state.cx:
   19:                         newbest = position
       
  270:                     new.append(
  270:                         State(
  135:                             i=(newi),
  135:                             j=(state.j),
  135:                             labels=(state.labels + "x"),
  135:                             cx=(newcx),
  135:                             cy=(state.cy),
  135:                             start=(state.start),
  135:                             end=(-1),
  135:                             best=(newbest),
                               )
                           )
  135:                     statesCreated += 1
       
                       # Symbol is next for Y
  222:                 if symbol == y[state.j]:
  131:                     newj = (state.j + 1) % len(y)
  131:                     newcy = state.cy + ((state.j + 1) // len(y))
  131:                     newbest = state.best
  131:                     if state.i == 0 and newj == 0 and newcy > state.cy:
   16:                         newbest = position
       
  262:                     new.append(
  262:                         State(
  131:                             i=(state.i),
  131:                             j=(newj),
  131:                             labels=(state.labels + "y"),
  131:                             cx=(state.cx),
  131:                             cy=(newcy),
  131:                             start=(state.start),
  131:                             end=(-1),
  131:                             best=(newbest),
                               )
                           )
  131:                     statesCreated += 1
       
                       # Symbol is not next for either X or Y, state ends
  222:                 if symbol != x[state.i] and symbol != y[state.j]:
   52:                     new.append(
   52:                         State(
   26:                             i=(state.i),
   26:                             j=(state.j),
   26:                             labels=(state.labels),
   26:                             cx=(state.cx),
   26:                             cy=(state.cy),
   26:                             start=(state.start),
   26:                             end=(position),
   26:                             best=(state.best),
                               )
                           )
   26:                     statesCreated += 1
       
   15:         symbolsProcessed += 1
       
               # Remove duplicate states after each symbol is processed
   15:         active = removeDuplicates(new)
       
    1:     results = ""
    1:     best = 0
   36:     for state in active:
   35:         if state.cx > 0 and state.cy > 0 and (state.best - state.start) > best:
    4:             best = state.best - state.start
    8:             results = (("n" * state.start) + state.labels)[: state.best + 1].ljust(
    4:                 len(s), "n"
                   )
       
           # Reorganize algorithm results
    1:     xSymbols = []
   16:     for position in range(len(results)):
   15:         if results[position] == "x":
    9:             xSymbols.append(position)
    4:     xSymbols = [list(xSignal) for xSignal in batched(xSymbols, len(x))]
       
    1:     ySymbols = []
   16:     for position in range(len(results)):
   15:         if results[position] == "y":
    6:             ySymbols.append(position)
    3:     ySymbols = [list(ySignal) for ySignal in batched(ySymbols, len(x))]
       
    1:     nSymbols = []
   16:     for position in range(len(results)):
   15:         if results[position] == "n":
>>>>>>             nSymbols.append(position)
       
           # Return
    1:     return xSymbols, ySymbols, nSymbols
       
       
>>>>>> def getArguments() -> argparse.Namespace:
           """Get the program's arguments"""
           # Program information
>>>>>>     parser = argparse.ArgumentParser(
>>>>>>         prog="Untangle-Signals",
>>>>>>         description="Executes the Untangle Signals algorithm",
           )
       
           # Run a trace
>>>>>>     parser.add_argument(
>>>>>>         "-t",
>>>>>>         "--trace",
>>>>>>         action="store_true",
>>>>>>         help="Runs a trace on the algorithm",
           )
       
           # Specify specific input file if desired
>>>>>>     parser.add_argument(
>>>>>>         "-i",
>>>>>>         "--input",
>>>>>>         action="store",
>>>>>>         default="input.txt",
>>>>>>         help="Specify input file name; can be used for specific inputs when the "
               "'P' flag is omitted, otherwise will save input file to given location",
           )
       
           # The s signal to use in the Untangle Signals algorithm
>>>>>>     parser.add_argument(
>>>>>>         "-s",
>>>>>>         "--s",
>>>>>>         action="store",
>>>>>>         default="",
>>>>>>         help="The s signal to use in the Untangle Signals algorithm",
           )
       
           # The x signal to use in the Untangle Signals algorithm
>>>>>>     parser.add_argument(
>>>>>>         "-x",
>>>>>>         "--x",
>>>>>>         action="store",
>>>>>>         default="",
>>>>>>         help="The x signal to use in the Untangle Signals algorithm",
           )
       
           # The y signal to use in the Untangle Signals algorithm
>>>>>>     parser.add_argument(
>>>>>>         "-y",
>>>>>>         "--y",
>>>>>>         action="store",
>>>>>>         default="",
>>>>>>         help="The y signal to use in the Untangle Signals algorithm",
           )
       
           # Output file
>>>>>>     parser.add_argument(
>>>>>>         "-o",
>>>>>>         "--output",
>>>>>>         action="store",
>>>>>>         default="output.txt",
>>>>>>         help="Specify output file name",
           )
       
>>>>>>     return parser.parse_args()
       
       
>>>>>> def main(args: argparse.Namespace) -> None:
    1:     s: str = ""
    1:     x: str = ""
    1:     y: str = ""
       
           # Save provided inputs if all are given
    1:     if args.s and args.x and args.y:
    2:         with open(args.input, "w") as f:
    1:             print((args.s, args.x, args.y), file=f)
       
           # Input
    1:     try:
    2:         with open(args.input, "r") as f:
    2:             for line in f.readlines():
    1:                 s, x, y = eval(line)
>>>>>>     except FileNotFoundError:
>>>>>>         print(f"Could not find {args.input}")
>>>>>>         exit()
       
           # Run the Untangle Signals algorithm
    1:     xSymbols, ySymbols, nSymbols = untangleSignals(s, x, y)
       
           # Print algorithm results
    1:     print(f"Number of States Sifted for Duplicates: {duplicatesSifted}")
    1:     print(f"Number of States Created: {statesCreated}")
    1:     print(f"Number of Symbols Processed: {symbolsProcessed}")
       
           # Save the output
    2:     with open(args.output, "w") as f:
    1:         print("X: " + str(xSymbols), file=f)
    1:         print("Y: " + str(ySymbols), file=f)
    1:         print("N: " + str(nSymbols), file=f)
       
       
>>>>>> if __name__ == "__main__":
           # Get the program's arguments
>>>>>>     args = getArguments()
       
           # Run a trace
>>>>>>     if args.trace:
               # Create Trace object
>>>>>>         tracer = trace.Trace(
>>>>>>             ignoredirs=[],
>>>>>>             trace=0,
>>>>>>             count=1,
               )
       
               # Run trace
>>>>>>         tracer.run("main(args)")
       
               # Make a report
>>>>>>         r = tracer.results()
>>>>>>         r.write_results(show_missing=True, coverdir="traceOutput")
       
           # Run normally
           else:
>>>>>>         main(args)
